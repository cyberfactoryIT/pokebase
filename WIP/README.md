# WIP - Work In Progress

Questa cartella contiene file temporanei, di test e backup spostati dalla root per pulizia.

## Contenuto

### File di Debug
- `debug_*.php` - Script di debug vari

### File di Test
- `test_*.php` - Script di test per API e funzionalità

### File di Check
- `check_*.php` - Script per verificare stato database e mappings

### Script di Import/Utility
- `populate_*.php` - Script per popolare dati
- `find_*.php` - Script di ricerca
- `map_*.php` - Script di mapping
- `simulate*.sh` - Script di simulazione pipeline
- `import_*.sh` - Script di import batch
- `monitor_*.sh` - Script di monitoring
- Altri script bash di utility

### File Vuoti/Temporanei
- `count`, `id`, `name`, `getMessage`, `pricing_plan_id` - File vuoti temporanei

### Backup
- `.env.backup.*` - Backup file environment

### Log
- `import.log` - Log import vecchi

## Nota

Questi file sono stati mantenuti per reference ma possono essere eliminati se non più necessari.
Per script attivi (sync, deploy) vedi la cartella `/scripts/`.
